@model CampusManagement.Models.ExamDate
@using CampusManagement.Models
@using (Html.BeginForm("Create", "ExamDates", FormMethod.Post))
{
    @Html.AntiForgeryToken()
    <div class="row">
        <h1>
            <span class="col-sm-12">
                @{ int hdnEid = ViewBag.hdnExamID;
                    ModelCMSNewContainer dbc = new ModelCMSNewContainer();
                    Exam nt = dbc.Exams.FirstOrDefault(bp => bp.ExamID == hdnEid);
                    @Html.Raw(nt.ExamTitle)
                } :
            </span>
        </h1>
    </div>

    <div class="row">
        <h2><span class="col-sm-12">Add Days</span></h2>
    </div>
    <hr style="border-top: 1px solid #6d9b90;" />
    @Html.Partial("_MessageView")
   
    <div class="row">

        <div class="col-sm-4 hidden">
            <label>Exam</label>
            @Html.DropDownList("ExamID", null, htmlAttributes: new { @class = "form-control" })
            @Html.ValidationMessageFor(model => model.ExamID, "", new { @class = "text-danger" })
        </div>
      
        <div class="col-sm-3">
            @Html.LabelFor(model => model.ExamDateTitle, htmlAttributes: new { @class = "control-label" })
            @Html.EditorFor(model => model.ExamDateTitle, new { htmlAttributes = new { @class = "form-control", @type = "date" , @required= true} })
            @Html.ValidationMessageFor(model => model.ExamDateTitle, "", new { @class = "text-danger" })

        </div>
      
        <div class="col-sm-3">
            @Html.LabelFor(model => model.IsActive, htmlAttributes: new { @class = "control-label" })
            @Html.DropDownList("IsActive", null, htmlAttributes: new { @class = "form-control" })
            @Html.ValidationMessageFor(model => model.IsActive, "", new { @class = "text-danger" })
        </div>
    </div>
    <br />
    <div class="row">
        <div class="col-sm-12">
            <input type="submit" value="Add" class="btn btn-primary" />
            @Html.ActionLink("Refresh", "Index", "ExamDates", new { ExamID =ViewBag.hdnExamID }, new {  @class="btn btn-primary"})
            @Html.ActionLink("Back to Exam", "Index", "Exams", new { ExamID = ViewBag.hdnExamID }, new { @class = "btn btn-primary" })
            
        </div>
    </div>
}
